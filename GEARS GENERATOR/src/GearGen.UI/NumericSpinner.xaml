<UserControl x:Class="GearGen.UI.NumericSpinner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:GearGen.UI">
    <UserControl.Resources>
        <Style x:Key="SpinBtn" TargetType="RepeatButton">
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="FontSize" Value="9"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Delay" Value="400"/>
            <Setter Property="Interval" Value="60"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#E5E7EB"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#D1D5DB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <StackPanel Margin="0,0,0,10">
        <DockPanel LastChildFill="False" Margin="0,0,0,3">
            <TextBlock Text="{Binding Label, RelativeSource={RelativeSource AncestorType=local:NumericSpinner}}"
                       Foreground="#374151" FontSize="12" DockPanel.Dock="Left"/>
            <TextBlock Text="{Binding SuffixDisplay, RelativeSource={RelativeSource AncestorType=local:NumericSpinner}}"
                       Foreground="#9CA3AF" FontSize="11" DockPanel.Dock="Right"/>
        </DockPanel>
        <Border BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="4" Background="White"
                x:Name="OuterBorder">
            <Grid Height="30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="22"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="PART_TextBox" Grid.Column="0" BorderThickness="0" Background="Transparent"
                         VerticalContentAlignment="Center" Padding="8,0,4,0" FontSize="13"
                         Foreground="#111827"
                         TextChanged="OnTextChanged" LostFocus="OnLostFocus" KeyDown="OnKeyDown"
                         PreviewMouseWheel="OnMouseWheel"/>
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <RepeatButton Grid.Row="0" Content="▲" Style="{StaticResource SpinBtn}" Click="OnUpClick"/>
                    <RepeatButton Grid.Row="1" Content="▼" Style="{StaticResource SpinBtn}" Click="OnDownClick"/>
                </Grid>
            </Grid>
        </Border>
    </StackPanel>
</UserControl>
